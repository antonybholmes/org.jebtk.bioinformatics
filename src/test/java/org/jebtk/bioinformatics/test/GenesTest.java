package org.jebtk.bioinformatics.test;

import java.io.IOException;

import org.junit.Test;

public class GenesTest {

  @Test
  public void webGenesTest() throws IOException {
    /*
     * UrlBuilder url =
     * UrlBuilder.fromUrl("http://156.145.14.157:8080/edbw8/api/v1");
     * //genes/grch38/chr3/1/187736
     * 
     * WebGenes genes = new WebGenes(url);
     * 
     * 
     * for (GenomicEntity g : genes.findGenes("gencode", "grch38", "chr3:1-187736"))
     * { System.err.println("found gene " + g);
     * 
     * for (GenomicEntity e : g.getChildren(GenomicType.EXON)) {
     * System.err.println("found exon " + e); } }
     */
  }
}
